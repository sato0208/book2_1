<div class="container">
	<h1>eding book</h1>
	<%= form_for(@book) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<%= f.label :body %>
	<%= f.text_area :body %>
	<%= f.submit 'Update Book' %>
	<%= link_to'show',book_path(@book.id) %>
	<%= link_to'Back',books_path %>
	<% end %>
</div>